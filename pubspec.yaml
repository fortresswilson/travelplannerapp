name: tropicaguide
description: "TropicaGuide — Collaborative Travel Planner App (Flutter + Firebase)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Google Fonts (optional — set fontFamily to 'Poppins' for polish)
  # google_fonts: ^6.2.1

  # Firebase — uncomment when Backend Lead sets up Firebase project
  # firebase_core: ^2.27.0
  # firebase_auth: ^4.17.8
  # cloud_firestore: ^4.15.8
  # firebase_storage: ^11.6.8
  # firebase_messaging: ^14.7.19
  # google_sign_in: ^6.2.1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
  #   - assets/icons/
